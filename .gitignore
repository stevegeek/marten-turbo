.DS_Store
.ideas/
.vscode/
/bin/
/lib/
/.shards/
/coverage/
/run_tests
*.dwarf
.icr_*
*.db
/shard.lock
.spec.env.json

# Per-machine override that points the `marten` dependency at a local
# path (../marten-src) for development. Don't ship to the fork.
/shard.override.yml